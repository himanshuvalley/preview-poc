version: 2.1

jobs:
  deploy-preview:
    docker:
      - image: cimg/node:18.20
    steps:
      - checkout
      - run:
          name: Check AWS env vars
          command: |
            if [ -z "$AWS_ACCESS_KEY_ID" ] || [ -z "$AWS_SECRET_ACCESS_KEY" ]; then
              echo "ERROR: Set AWS keys in CircleCI Project Settings → Environment Variables"
              exit 1
            fi
            export AWS_DEFAULT_REGION="${AWS_DEFAULT_REGION:-ap-south-1}"
            echo "Region: $AWS_DEFAULT_REGION"
      - run:
          name: Install dependencies
          command: npm install
      - run:
          name: Deploy preview
          command: |
            export AWS_DEFAULT_REGION="${AWS_DEFAULT_REGION:-ap-south-1}"

            # Prefer PR number so close/delete can use the same stage name
            if [ -n "$CIRCLE_PR_NUMBER" ]; then
              STAGE="pr-${CIRCLE_PR_NUMBER}"
            elif [ -n "$CIRCLE_PULL_REQUEST" ]; then
              STAGE="pr-$(echo "$CIRCLE_PULL_REQUEST" | grep -oE '[0-9]+$' )"
            else
              echo "ERROR: Open a Pull Request (not only push branch). PR number required."
              exit 1
            fi

            echo "Deploying stage: $STAGE"
            echo "STAGE=$STAGE" >> "$BASH_ENV"
            npx serverless deploy --stage "$STAGE" --verbose
            echo "Preview URL base is in Stack Outputs → HttpApiUrl (add /hello)"

workflows:
  preview:
    jobs:
      - deploy-preview:
          filters:
            branches:
              ignore: main
